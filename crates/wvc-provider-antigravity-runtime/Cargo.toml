[package]
name = "wvc-provider-antigravity-runtime"
version = "0.1.0"
edition = "2024"
description = "Antigravity provider runtime (Google Cloud Code multiplexed Gemini/Claude backend) for jcode, kept downstream of wvc-base so provider edits do not rebuild the app spine"

[lib]
name = "wvc_provider_antigravity_runtime"
path = "src/lib.rs"

[dependencies]
anyhow = "1"
async-trait = "0.1"
chrono = { version = "0.4", features = ["serde"] }
# default-features = false: the top-level binary decides heavy optional base
# features (embeddings/bedrock). Runtime crates must not re-enable them via
# feature unification, or --no-default-features release targets (e.g. Windows
# ARM64, which cannot build tract-linalg asm) break.
wvc-base = { path = "../wvc-base", default-features = false }
wvc-message-types = { path = "../wvc-message-types" }
wvc-provider-antigravity = { path = "../wvc-provider-antigravity" }
wvc-provider-core = { path = "../wvc-provider-core" }
wvc-provider-gemini = { path = "../wvc-provider-gemini" }
reqwest = { version = "0.12", default-features = false, features = ["json", "stream", "charset", "http2", "system-proxy", "rustls-tls", "rustls-tls-native-roots"] }
serde_json = "1"
tokio = { version = "1", features = ["sync", "time", "rt"] }
tokio-stream = "0.1"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
# The migrated antigravity tests use wvc-base's test-env sandbox.
wvc-base = { path = "../wvc-base", default-features = false, features = ["test-support"] }
tempfile = "3"
