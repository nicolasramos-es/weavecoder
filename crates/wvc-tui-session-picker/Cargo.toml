[package]
name = "wvc-tui-session-picker"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
wvc-message-types = { path = "../wvc-message-types" }
wvc-session-types = { path = "../wvc-session-types" }
serde = { version = "1", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]
