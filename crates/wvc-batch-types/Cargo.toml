[package]
name = "wvc-batch-types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
wvc-message-types = { path = "../wvc-message-types" }
serde = { version = "1", features = ["derive"] }
