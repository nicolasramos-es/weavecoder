[package]
name = "wvc-session-types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
wvc-message-types = { path = "../wvc-message-types" }
memchr = "2"
serde = { version = "1", features = ["derive"] }
