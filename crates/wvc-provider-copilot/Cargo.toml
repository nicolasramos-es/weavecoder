[package]
name = "wvc-provider-copilot"
version = "0.1.0"
edition = "2024"

[dependencies]
wvc-message-types = { path = "../wvc-message-types" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
