[package]
name = "wvc-plan"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tokenizers = { version = "0.21", default-features = false, features = ["onig"] }
