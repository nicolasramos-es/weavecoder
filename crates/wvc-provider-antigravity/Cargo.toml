[package]
name = "wvc-provider-antigravity"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
wvc-provider-gemini = { path = "../wvc-provider-gemini" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
